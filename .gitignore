# Rust
/target
**/*.rs.bk
Cargo.lock

# Java / Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
out/
bin/
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar
*.war
*.ear
hs_err_pid*
replay_pid*
/dist

# Local agent/dev scratch — per AGENTS.md, never commit or publish.
/docs_local

# Editors / IDEs
.idea/
.vscode/
.vs/
.zed/
*.code-workspace
.history/

# Editor backups
*.swp
*.swo
*~

# OS
.DS_Store
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# /wrush task worktrees — always created here, in-tree
/worktrees/

# Local-only cargo overrides (see switch_consumers_to_local_ktav.sh).
# Committing one makes the crate unbuildable everywhere else.
/.cargo/

# Node (root-docs doc tooling)
node_modules/
